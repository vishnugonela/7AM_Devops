pipeline {
    agent any

    stages {
        stage('build') {
            steps { 
               sh 'echo "build is successful"'
            }
        }
         stage('deploy') {
            steps {
                sh 'echo "build is successful"'
            }
        }
        stage('test'){
            steps{
                sh 'echo "build is successful"'
            }
        }
        
    }
}
