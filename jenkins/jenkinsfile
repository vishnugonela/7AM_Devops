pipeline {
    agent any
    stages {
        stage('Retrieve Server Details') {
            steps {
                script {
                    echo "Retrieving CPU and Memory Information:"
                    def cpuInfo = sh(script: 'lscpu')
                    def memoryInfo = sh(script: 'free -h')
                    def systemInfo = sh(script: 'uname -a')
                    echo "CPU Information:"
                    echo cpuInfo

                    echo "Memory Information:"
                    echo memoryInfo

                    echo "System Information:"
                    echo systemInfo
                }
            }
        }
    }
}
