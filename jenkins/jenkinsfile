pipeline {
    agent any
    stages {
        stage('Server Details') {
            steps {
                sh '''
                    echo "Server Details:"
                    echo "CPU Information:"
                    echo ${cpuInfo} : 
                    lscpu

                    echo "Memory Information:"
                    echo ${memoryInfo}
                    free -h

                    echo "System Information:"
                    echo ${systemInfo}
                    uname -a

                   '''
            }
        }
    }
}
